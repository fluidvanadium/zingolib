// pub mod wallet;

pub struct ZingoStore {
    // the actual data, a subset of the blockchain. implements WalletRead, WalletWrite, and WalletCommitmentTrees
    // wallet: ZingoWallet,
    // includes chainType/used servers, behavioral options, historical prices and dates for block times
    // context: ZingoContext,
    // seeds, keys, and secrets
    // capabilities: ZingoCapabilities,
    //
}

pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    // use super::*;

    // #[test]
    // fn it_works() {
    //     let result = add(2, 2);
    //     assert_eq!(result, 4);
    // }
}
